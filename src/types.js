/**
 * @typedef Task
 * @prop {number} id
 * @prop {string} contents
 * @prop {boolean} isDone
 * @prop {number} createdDate
 * @prop {number} modifiedDate
 */

/**
 * @typedef {Task & {selected: boolean; changedContents?: string}} ExtendedTask
 */

/**
 * @typedef DropdownItem
 * @prop {string} label
 * @prop {string | number} value
 */
